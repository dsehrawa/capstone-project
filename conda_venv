# 1. Create a new conda environment
conda create -n myenv python=3.10

# 2. Activate it
conda activate myenv

# 3. Install pip (if not already present)
conda install pip

# 4. Install requirements.txt
pip install -r requirements.txt
